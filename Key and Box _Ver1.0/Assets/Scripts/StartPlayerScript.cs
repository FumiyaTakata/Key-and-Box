using UnityEngine;
using System.Collections;

public class StartPlayerScript : MonoBehaviour {
	//RoomManagerScript manager = new RoomManagerScript();
	// Use this for initialization
	void Start () {
		//manager.Awake();
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
